**AUDITORÍA TÉCNICA - Lógica de Backend para Gestión Colaborativa de Años y Validación de Cortes (12/02/2026)**

**Archivo: ui.js**
- Función `mostrarDetalleModal`: Renderiza la imagen del vehículo (`img-vehiculo-modal`) y las secciones de cortes.
- El requisito pide mostrar la notificación debajo de la imagen del modelo del vehículo.
- El momento de aparición debe ser tras la carga de la imagen del corte principal.
- Riesgo: Si no hay imagen de corte, ¿cuándo aparece? El requisito dice "después de que la imagen del corte principal del vehiculo haya terminado de cargar".
- Necesidad: Implementar un sistema de notificaciones integrado (no modales del sistema).

**Archivo: feedback.js (Backend)**
- Función `handleSuggestYear`: Ya implementa una lógica de votación (3 votos) y actualización de `anoDesde`/`anoHasta`.
- Requisito: "Cuando existan más de dos confirmaciones" (es decir, 3 o más). `voteCount < 3` en el código actual coincide.
- Requisito: "La columna anoSugerido deberá contener el año actual."
- Necesidad: Verificar si la estructura de la hoja `SugerenciasAño` coincide con lo que el frontend enviará.
- Necesidad: Asegurar que el registro sea silencioso y no bloqueante.

**Archivo: offline.js**
- Necesidad: Añadir un nuevo ObjectStore (`validationCache`) para persistir que un usuario ya respondió a una validación para un `vehicleId` específico.

**Archivo: api-config.js**
- Necesidad: Añadir helper `suggestYear` para llamar a la acción `suggestYear` del servicio `FEEDBACK`.

**Archivo: state.js**
- Podría ser necesario mantener el estado de las validaciones respondidas en la sesión actual, aunque `offline.js` es preferible para persistencia entre sesiones.

**Hallazgos Generales:**
- Lógica de "generación más reciente" se calcula en el frontend basándose en `catalogData.cortes`.
- Se compara Marca, Modelo, Tipo de Encendido y Categoría para agrupar generaciones.
- El año actual se calcula mediante `new Date().getFullYear()`.

--------------------------------------------------

--------------------------------------------------
**AUDITORÍA TÉCNICA - Diagnóstico de bug en apertura automática del modal (24/06/2026)**

**Archivos involucrados:** ui.js, navigation.js, main.js.

**Hallazgos:**
1. **ui.js (mostrarResultadosDeBusqueda):**
   - El bloque condicional para resultados únicos (`results.length === 1`) invoca a `mostrarDetalleModal` pero realiza un `return` inmediato.
   - Este `return` interrumpe el flujo de renderizado, impidiendo que se cree la cuadrícula (`grid`) y la tarjeta (`card`) del vehículo en la vista de resultados.
   - Impacto: El usuario ve el modal (si no se cierra) pero al cerrarlo la lista de resultados está vacía (solo el título).

2. **navigation.js (filtrarContenido):**
   - La actualización del hash de la URL (`window.location.hash`) ocurre al final de la función, después de haber renderizado los resultados y abierto el modal.
   - El cambio de hash dispara un evento `popstate` que es procesado por el listener global en `main.js`.

3. **main.js (popstate listener):**
   - El listener de `popstate` closes automáticamente cualquier modal de detalle visible (`#modalDetalle.visible`) para soportar la navegación con el botón "Atrás".
   - Debido a que el hash se actualiza después de abrir el modal, el evento `popstate` resultante cierra el modal inmediatamente después de que se abre.
   - Esto explica por qué el vehículo se registra en "Vistos recientemente" (the función se ejecuta) pero el modal no permanece visible.

**Acciones Correctoras:**
- **ui.js:** Eliminar el `return` en el bloque de resultado único de `mostrarResultadosDeBusqueda` para permitir el renderizado completo de la UI.
- **navigation.js:** Reordenar la ejecución para actualizar el hash de la URL antes de invocar el renderizado de resultados, evitando la colisión de eventos de historial con la apertura del modal.

--------------------------------------------------

--------------------------------------------------
**AUDITORÍA TÉCNICA - Gestión automática del teclado y historial de navegación (07/07/2026)**

**Archivos involucrados:** main.js, ui.js, navigation.js.

**Hallazgos:**
1. **Gestión del Foco (ui.js / main.js):**
   - En `ui.js`, `mostrarResultadosDeBusqueda` abre el modal automáticamente si hay un solo resultado, pero no retira el foco de `searchInput`.
   - El teclado virtual se mantiene abierto debido a que `searchInput` conserva el foco.
   - En `main.js`, el listener de `popstate` cierra el modal pero no restaura el foco a la barra de búsqueda de forma explícita en todos los flujos de cierre.

2. **Teclado Virtual:**
   - No hay bloqueos explícitos, el comportamiento depende enteramente del foco de los elementos input.
   - El uso de `visualViewport` en `main.js` ya gestiona clases como `keyboard-open`, lo cual es bueno para el layout.

3. **Navegación e Historial (main.js / navigation.js):**
   - El flujo de "segundo retroceso" requiere que, estando en resultados de búsqueda (sin modal), el siguiente "Atrás" limpie la búsqueda y restaure el catálogo.
   - Actualmente, `main.js` en `popstate` intenta restaurar la búsqueda si `state.query` existe. Esto podría entrar en conflicto con el deseo de "retroceder" fuera de la búsqueda.
   - `navigation.irAPaginaPrincipal` ya realiza limpiezas de hash y estado, pero debe integrarse correctamente en el flujo de gestos.

4. **Compatibilidad:**
   - El uso de `document.activeElement.blur()` and `input.focus()` es estándar y compatible con PWA y navegadores móviles.

**Riesgos y Dependencias:**
- La animación del encabezado depende de la clase `search-active` en el body. No debe alterarse manualmente si no es a través de los flujos de búsqueda existentes.
- El retraso de 150ms en la apertura automática del modal (`ui.js`) es crítico para evitar colisiones con el renderizado del grid.


--------------------------------------------------
**AUDITORÍA TÉCNICA - Sincronización de Historial y Navegación del Dispositivo (07/07/2026)**

**Archivos involucrados:** ui.js, main.js, navigation.js.

**Hallazgos:**
1. **Inconsistencia de pushState (ui.js):**
   - Funciones como `mostrarMarcas`, `mostrarModelosPorMarca`, `mostrarModelos`, `mostrarTiposEncendido`, `mostrarVersiones` y `mostrarVersionesEquipamiento` actualizan el estado interno (`setState`) pero NO ejecutan `history.pushState`.
   - Esto causa que el botón Atrás del dispositivo ignore estos niveles y salte directamente al último estado registrado (usualmente el inicio o la búsqueda inicial).
   - El botón interno "< Volver" funciona porque usa llamadas directas a funciones, no depende del historial del navegador.

2. **Cierre de Modal y Historial:**
   - `mostrarDetalleModal` realiza un `pushState({ modalOpen: true })`.
   - Al cerrar el modal con "X", se llama a `history.back()`. Si el usuario ya estaba en el tope de la pila (ej. recarga de página con modal abierto), `history.back()` podría intentar salir de la aplicación.
   - El cierre manual debe ser consistente con el estado del historial.

3. **Reapertura de Modal:**
   - Se debe asegurar que al retroceder desde un nivel a otro, si el nivel anterior tenía un modal abierto "automáticamente" (resultado único), este no se vuelva a abrir si ya fue cerrado. La bandera `autoOpen` implementada en v2.1.5 debe preservarse y reinforced.

4. **Navegación Agrupada (Búsqueda):**
   - Al navegar desde resultados de búsqueda hacia niveles internos (ej. modelos de una marca encontrada), se debe registrar cada paso en el historial para permitir el retroceso granular solicitado.

**Acciones Planificadas:**
- Implementar `pushState` en todas las funciones de cambio de nivel en `ui.js`.
- Actualizar el listener de `popstate` en `main.js` para manejar la reconstrucción de vistas basadas en el estado del historial (level, marca, modelo, categoria, etc.).
- Asegurar que `irAPaginaPrincipal` sea el ancla base del historial.

### CORRECCIÓN DE NAVEGACIÓN Y TECLADO VIRTUAL
- Implementada interceptación de popstate para cerrar el teclado virtual mediante blur() sin retroceder en el historial en el primer gesto.
- Agregado el campo 'origin' ('marca' o 'categoria') en el navigationState and en history.pushState para mantener independientes los contextos de navegación y evitar mezclar rutas en el retroceso.

--------------------------------------------------
**AUDITORÍA TÉCNICA - CORRECCIÓN AJUSTE POST-COMMIT DE HISTORIAL (14/07/2026)**

**Archivo: main.js (popstate)**
- **Hallazgo:** Cuando el input de búsqueda está enfocado y se realiza un retroceso, el navegador ya ha retrocedido un estado en el historial (dejando activo el estado anterior, p. ej. 'categorias').
- El código actual re-insertaba el estado actual ya modificado (), duplicando el estado de la página de inicio en lugar de preservar el estado de búsqueda activo (). Esto causaba que la búsqueda se borrara del historial y el siguiente retroceso cerrara la aplicación.
- **Acción:** Re-insertar explícitamente el estado de búsqueda  reconstruido dinámicamente con el valor del input, preservando el historial y permitiendo que el segundo retroceso regrese correctamente a la página principal.

--------------------------------------------------
**AUDITORÍA TÉCNICA - CORRECCIÓN AJUSTE POST-COMMIT DE HISTORIAL (14/07/2026)**

**Archivo: main.js (popstate)**
- **Hallazgo:** Cuando el input de búsqueda está enfocado y se realiza un retroceso, el navegador ya ha retrocedido un estado en el historial (dejando activo el estado anterior, p. ej. "categorias").
- El código anterior re-insertaba el estado actual ya modificado (state), duplicando el estado de la página de inicio en lugar de preservar el estado de búsqueda activo ({ level: "busqueda", query: currentQuery }). Esto causaba que la búsqueda se borrara del historial y el siguiente retroceso cerrara la aplicación.
- **Acción:** Re-insertar explícitamente el estado de búsqueda { level: "busqueda", query: currentQuery } reconstruido dinámicamente con el valor del input, preservando el historial y permitiendo que el segundo retroceso regrese correctamente a la página principal.

--------------------------------------------------
**AUDITORÍA TÉCNICA - REFINAMIENTO POST-COMMIT DEL HISTORIAL DE BÚSQUEDA (14/07/2026)**

**Archivos: main.js, ui.js, navigation.js**
- **Hallazgo:** Se identificó que la barra de búsqueda en estado enfocado (teclado abierto) y desenfocado (teclado cerrado) compartían el mismo estado de historial "busqueda". Esto causaba una duplicación en la pila de historial cuando el usuario utilizaba el gesto de retroceso para desenfocar la barra de búsqueda, impidiendo regresar a la página principal en el segundo retroceso.
- **Acción:**
  1. Introducido el nuevo estado de historial "busqueda_focused" para representar la barra de búsqueda enfocada / teclado abierto.
  2. Al enfocar o escribir, el estado se establece en "busqueda_focused".
  3. Al desenfocar (blur), el estado se reemplaza silenciosamente con "busqueda" (desenfocado).
  4. En el primer retroceso (si está enfocado), se ejecuta blur() de inmediato, se elimina la clase "search-active" sin desfase de tiempo, y se re-inserta el estado "busqueda".
  5. En el segundo retroceso, se vuelve correctamente al estado inicial sin duplicaciones ni cierres de aplicación.
  6. Soportados ambos escenarios (Resultados múltiples y Modal de resultado único) con perfecta correspondencia en la pila de navegación del navegador y gestos del sistema.


--------------------------------------------------
**AUDITORÍA TÉCNICA - REFINAMIENTO DEL FLUJO DE RETROCESO (15/07/2026)**

**Archivos involucrados:** main.js, navigation.js, ui.js.

**Hallazgos:**
1. **Inicialización del Historial:**
   - La aplicación no inicializa `window.history.state` en el arranque, dejando estados `null` iniciales. Esto puede provocar que al retroceder se cierre la aplicación de forma abrupta.
   - Existen llamadas a `replaceState(null, ...)` en `navigation.js` and `ui.js` que rompen la homogeneidad del historial. Deberán ser reemplazadas por `{ level: "categorias" }`.

2. **Gestión de Foco en Retroceso:**
   - Algunos navegadores móviles desenfocan automáticamente los campos de entrada antes de disparar el evento `popstate`. Por ello, comprobar únicamente `document.activeElement === searchInput` no es robusto.
   - Solución: Incorporar la verificación de `navigationState.level === "busqueda_focused"` para interceptar de manera confiable el primer retroceso desde una búsqueda activa, incluso si el foco físico ya se perdió en el dispositivo.

3. **Teclado, Foco y Transición Visual:**
   - La transición visual de salida (quitar `.search-active`) tiene un retardo de 250ms en el listener de `blur` del input de búsqueda para permitir clics en el botón "X".
   - Sin embargo, para la navegación por gestos de retroceso, se requiere que la animación inversa, la pérdida de foco y la ocultación del teclado formen parte de una única transición inmediata.
   - Solución: Introducir una variable `isBackGestureBlurring` para evitar el retardo del listener de `blur` durante la ejecución del retroceso, logrando una transición instantánea y unificada.

4. **Simplificación de Restauración:**
   - El bloque de restauración final en `popstate` verifica hashes de forma que los hashes residuales podrían bloquear la limpieza.
   - Solución: Simplificar el bloque para que invoque a `irAPaginaPrincipal(true)` siempre que `!state.query`.

--------------------------------------------------
**AUDITORÍA TÉCNICA - REFINAMIENTO DE NAVEGACIÓN Y HISTORIAL DE BÚSQUEDA (15/07/2026)**

**Archivos involucrados:** main.js, navigation.js, ui.js.

**Hallazgos:**
1. **Lógica de Doble y Triple Retroceso**:
   - Actualmente, en `focus` de la barra de búsqueda, se insertan dos estados (`busqueda` y `busqueda_focused`).
   - Al retroceder del primer estado (`busqueda_focused`), el popstate intercepta el retroceso y vuelve a empujar el estado `busqueda`, lo que resulta en un historial duplicado o hinchado que impide la correcta correspondencia de 1 gesto por transición.
   - Para que el primer retroceso funcione exactamente como una transición visual unificada (ocultar teclado, perder foco, animación inversa), pero sin abandonar los resultados ni la consulta actual, se debe garantizar que el historial tenga exactamente un estado `busqueda_focused` por encima del estado anterior (`categorias` o `modelosPorMarca`).
   - Al ocurrir el retroceso de `busqueda_focused`, el navegador ya regresa de forma nativa al estado anterior (ej. `categorias` o `modelosPorMarca`). En este popstate, capturamos que veníamos de `busqueda_focused`, desactivamos el foco/teclado/search-active, y empujamos un nuevo estado `busqueda` desenfocado en el historial. Esto mantiene la consulta y resultados en pantalla.
   - En el segundo retroceso (desde `busqueda` desenfocado), el navegador vuelve de manera nativa al estado inicial (ej. `categorias` o `modelosPorMarca`). Al procesar ese popstate, dado que `currentNavState.level === 'busqueda'`, se limpia la búsqueda, se eliminan los resultados, y se restaura completamente el estado inicial de la aplicación.
   - Esto reduce drásticamente la pila de historial redundante y sincroniza perfectamente la navegación de gestos del sistema.

2. **Sincronización del Modal Automático (Escenario 2)**:
   - Al abrirse automáticamente el modal por resultado único, se añade una entrada de historial (`modalOpen: true`).
   - El primer retroceso cierra el modal de detalles de manera determinista y restaura el foco en el buscador (teclado visible, estado `busqueda_focused`).
   - El segundo retroceso realiza la transición visual unificada (ocultar teclado, perder foco, animación inversa, estado `busqueda`).
   - El tercer retroceso limpia la búsqueda y regresa por completo al estado inicial.

**Acciones Planificadas:**
- Limpiar y simplificar la lógica de inserción de estados en `focus` de `main.js`.
- Actualizar el listener de `popstate` para procesar limpiamente las transiciones del historial.

--------------------------------------------------
**AUDITORÍA TÉCNICA - CAUSA RAÍZ DE NAVEGACIÓN FALLIDA (15/07/2026)**

**Archivos involucrados:** navigation.js, main.js.

**Causa Raíz de la Falla:**
- Al borrar el contenido del buscador (por tecleo de retroceso o haciendo clic en "X"), `filtrarContenido("")` invocaba directamente a `irAPaginaPrincipal()`.
- `irAPaginaPrincipal()` ejecuta `history.replaceState({ level: 'categorias' })` para limpiar los parámetros de la URL, removiendo la clase `search-active` y haciendo `blur()` del input.
- Sin embargo, en el caso del botón "X" o si el usuario sigue con el foco puesto en la barra de búsqueda, el input se re-enfocaba, pero el historial del navegador ya había perdido el estado de búsqueda por completo (reemplazado por `categorias`).
- Al estar en el estado `categorias` en la pila del historial, cualquier gesto de retroceso posterior del sistema operativo cerraba la aplicación abruptamente porque no quedaba ningún estado anterior al cual regresar en el navegador.

**Solución Definida:**
- En `navigation.js` (`filtrarContenido`), si la consulta se vuelve vacía (`!busqueda`), comprobar si se trata de una restauración (`isRestoring`).
- Si **NO** se trata de una restauración (`!isRestoring`), se llama directamente a `mostrarCategorias()` para limpiar los resultados de búsqueda visuales y mostrar el catálogo, pero **SE CONSERVA** el estado de búsqueda activo (`busqueda_focused` or `busqueda` con query vacía) mediante `history.replaceState({ level, query: "" })`.
- Esto mantiene el teclado virtual abierto y la clase `search-active` activa de manera fluida, garantizando que el historial del navegador retenga el estado de búsqueda para responder perfectamente a un retroceso del dispositivo.
- Si **SÍ** se trata de una restauración (`isRestoring`), se llama a `irAPaginaPrincipal(true)` de forma normal para limpiar el input de búsqueda y regresar al estado inicial.

--------------------------------------------------
**AUDITORÍA TÉCNICA - RESOLUCIÓN DEFINITIVA DE HISTORIAL NATIVO (15/07/2026)**

**Archivos involucrados:** main.js, navigation.js.

**Por qué la estrategia anterior falló:**
- En la iteración anterior, se intentó optimizar el historial haciendo que el foco en el buscador solo empujara un único estado (`busqueda_focused`).
- Al realizar el primer gesto de retroceso, el navegador regresa físicamente al estado anterior (ej. `categorias`). Para evitar la pérdida de los resultados de búsqueda, el controlador `popstate` intentaba re-insertar el estado desenfocado haciendo `history.pushState({ level: "busqueda" })`.
- Sin embargo, llamar a `pushState` de forma asíncrona dentro de un manejador de evento `popstate` is altamente inestable y frecuentemente bloqueado o ignorado por los motores de navegación de dispositivos móviles (Android Chrome, iOS Safari) y motores de PWA durante la fase de transición de gestos.
- Al fallar o ignorarse el `pushState` asíncrono, el historial se quedaba únicamente con el estado base (`categorias`). Por lo tanto, el segundo gesto de retroceso cerrará la aplicación de forma prematura.

**Nueva Estrategia de Historial Forward-Stack 100% Nativo:**
- **Al enfocar (focus)**: Empujar de forma secuencial y por adelantado el estado `busqueda` (desenfocado) y luego el estado `busqueda_focused` (enfocado). Esto construye una pila de historial legítima y nativa de 3 estados antes de que el usuario comience a navegar hacia atrás:
  1. `categorias` (o estado previo)
  2. `busqueda` (desenfocado)
  3. `busqueda_focused` (enfocado)
- **Al realizar el primer retroceso**: El navegador regresa de forma nativa a la entrada 2 (`busqueda`). El manejador del `popstate` intercepta esto, oculta el teclado, pierde el foco y realiza la animación inversa de forma visual. Para sincronizar la consulta actual sin alterar la pila de historial ni recurrir a `pushState`, se usa `history.replaceState({ level: "busqueda", query: currentQuery })`.
- **Al realizar el segundo retroceso**: El navegador regresa de forma nativa a la entrada 1 (`categorias` o estado previo). El manejador del `popstate` limpia el buscador y restaura la pantalla inicial completamente.


===================================================================
AUDITORÍA DE MEJORAS DE FRONTEND (SIN MODIFICACIONES AL BACKEND) (17/02/2026)
===================================================================

-------------------------------------------------------------------
DIAGNÓSTICO TÉCNICO Y HALLAZGOS DETALLADOS
-------------------------------------------------------------------

ID del hallazgo: H-FRONT-01
Título: Falta de opción para indicar activación del testigo del motor en formulario de cortes
Descripción: En `add_cortes.html` (Etapa 2), los usuarios no disponen de una opción rápida para indicar si un corte activa el testigo del motor. El requisito exige un checkbox que inyecte un bloque HTML específico al final de la descripción del campo "Ubicación del Corte" y lo remueva de manera limpia y sin duplicarse si se desmarca.
Ubicación:
  - Archivo: add_cortes.html
Impacto: Medio
Riesgo: Bajo
Dependencias: Campo `ubicacionCorte1`

ID del hallazgo: H-FRONT-02
Título: Determinación inexacta de generación más reciente y validación de año laxa
Descripción:
  1. En `ui.js` (`checkValidationEligibility`), la comparación de la generación más reciente asume que el ID del vehículo debe coincidir exactamente con el ID obtenido por `reduce` de la generación con mayor año de inicio. Si existen múltiples cortes registrados para la misma última generación, la comparación de IDs falla erróneamente para algunos registros. Debemos validar que no exista ninguna generación posterior para el mismo vehículo.
  2. En `ui.js` (`showStepInput`), la validación del año introducido por el usuario permite letras en navegadores que no bloquean la entrada, permite valores no numéricos parciales y tolera años futuros de hasta `currentYear + 2`. El requisito exige restringir a números estrictos de 4 dígitos, con un máximo de `currentYear + 1` y mínimo de `1980` (rango esperado por el sistema).
Ubicación:
  - Archivo: ui.js
Impacto: Alto
Riesgo: Medio
Dependencias: `checkValidationEligibility`, `showValidationBanner`

-------------------------------------------------------------------
PLAN DE RESPALDO OBLIGATORIO
-------------------------------------------------------------------
Archivos a respaldar:
- `add_cortes.html` -> `add_cortes.html.bak`
- `ui.js` -> `ui.js.bak`

===================================================================
AUDITORÍA DE MEJORAS DE FRONTEND - ADICIONAL "ES MÁS ANTIGUO" (17/02/2026)
===================================================================

ID del hallazgo: H-FRONT-03
Título: El botón "Es más antiguo" no solicita el año al usuario y no se muestra para modelos antiguos
Descripción:
  1. En `ui.js` (`showStep1`), el botón "Es más antiguo" solo se muestra cuando `isOldModel` es falso (`if (!isOldModel)`). Sin embargo, incluso para modelos antiguos, el usuario puede estar trabajando en una versión aún más antigua, por lo que la opción debe estar disponible siempre.
  2. Al presionar "Es más antiguo", el sistema invoca inmediatamente `registerAndClose` con un año precalculado, en lugar de solicitar el año específico mediante un campo de entrada (input text) como solicita el usuario.
Estrategia de corrección:
  - Hacer que el botón "Es más antiguo" se muestre siempre en `showStep1` indepeendientemente de `isOldModel`.
  - Modificar su acción para invocar `showStepInput(true)`, permitiendo al usuario ingresar el año específico para la opción "Es más antiguo".
  - Actualizar `showStepInput` para aceptar el parámetro `isFromAntiguo` y registrar la respuesta adecuada.

===================================================================
AUDITORÍA DE MEJORAS DE FRONTEND (VERSIÓN 2.1.5) (18/02/2026)
===================================================================

ID del hallazgo: H-FRONT-v2.1.5-01
Título: Ausencia de versión de equipamiento en coincidencias del formulario de nuevos cortes
Descripción: Durante la Etapa 1.5 en `add_cortes.html`, se listan coincidencias de vehículos similares para evitar duplicados, pero no se indica la versión de equipamiento (`match.versionesAplicables`). Adicionalmente, el sistema debe ser capaz de comparar y emparejar versiones agrupadas de forma flexible (ej. 'LX / EX / Sport' vs 'Sport, LX, EX') sin importar el orden, casing o delimitadores.
Ubicación:
  - Archivo: `add_cortes.html` (displayMatches)
Impacto: Medio
Riesgo: Bajo
Estrategia de corrección:
  - Mostrar la versión del vehículo en la match card.
  - Implementar un normalizador de versiones similar a `normalizeVersion` de `ui.js` para realizar una comparación de compatibilidad e indicar si coincide con la versión ingresada por el usuario.

ID del hallazgo: H-FRONT-v2.1.5-02
Título: Orden incorrecto de z-index en Lightbox de configuración de relay
Descripción: Cuando se abre el modal secundario de configuración de relay desde el modal de detalle, abrir el Lightbox del diagrama técnico lo muestra por detrás del modal de relay debido a que el z-index de `.lightbox` (1700) es menor que el de `.info-modal` (2100).
Ubicación:
  - Archivo: `style.css` (.lightbox)
Impacto: Alto
Riesgo: Muy bajo
Estrategia de corrección:
  - Incrementar el z-index de `.lightbox` a 2200 para que se superponga correctamente a los modales de información y detalle.

ID del hallazgo: H-FRONT-v2.1.5-03
Título: Falta de navegación interactiva desde logotipo de marca en modal de detalle
Descripción: El logotipo de la marca mostrado en el encabezado del modal de detalle es estático. Al hacer clic debería cerrar el modal y navegar al catálogo mostrando únicamente los modelos pertenecientes a esa marca.
Ubicación:
  - Archivo: `ui.js` (mostrarDetalleModal)
Impacto: Medio
Riesgo: Bajo
Estrategia de corrección:
  - Hacer clickable el logotipo de marca con un cursor `pointer`.
  - En su evento de clic, cerrar el modal, actualizar el estado de historial a `modelosPorMarca` (vía replaceState) y ejecutar `mostrarModelosPorMarca` para la marca correspondiente.

ID del hallazgo: H-FRONT-v2.1.5-04
Título: Falta de navegación desde categoría del vehículo en modal de detalle
Descripción: La categoría del vehículo mostrada debajo de la marca/modelo en el modal de detalle es estática. Al hacer clic debería cerrar el modal y filtrar los modelos de esa marca que correspondan a esa categoría específica.
Ubicación:
  - Archivo: `ui.js` (mostrarDetalleModal)
Impacto: Medio
Riesgo: Bajo
Estrategia de corrección:
  - Convertir el texto de categoría en un elemento de navegación interactivo clickable.
  - Al hacer clic, cerrar el modal, actualizar el estado de navegación a `modelos` (vía replaceState) y ejecutar `mostrarModelos` para filtrar por marca y categoría.

ID del hallazgo: H-FRONT-v2.1.5-05
Título: Falta de navegación rápida entre generaciones en el modal de detalle
Descripción: No existe una vía rápida para saltar entre generaciones del mismo modelo de vehículo desde el modal de detalle, lo que obliga al usuario a volver atrás y re-navegar.
Ubicación:
  - Archivo: `ui.js` (mostrarDetalleModal)
Impacto: Alto
Riesgo: Medio
Estrategia de corrección:
  - Identificar todas las generaciones del mismo modelo del vehículo (coincidiendo en marca, modelo, categoría, tipo de encendido y versión normalizada) ordenadas de forma ascendente por año de inicio (`anoDesde`).
  - Añadir flechas navegables `<` y `>` al lado del rango de años si existen múltiples generaciones.
  - Implementar acciones en las flechas que llamen a `mostrarDetalleModal` para la generación anterior o siguiente, enviando un parámetro `isNavigation = true` para actualizar la vista sin agregar nuevos registros repetidos a la pila del historial.

ID del hallazgo: H-NAV-v2.1.5-06
Título: Comportamiento intermitente en la navegación por marcas
Descripción: Se ha identificado un problema de inconsistencia en la navegación por marcas desde el carrusel de la página de inicio, resultados de búsqueda y detalles del modal. El problema ocurre de manera intermitente debido a la falta de normalización y homogeneización de los nombres de marcas, modelos y categorías que se cargan desde el catálogo central (Google Sheets o IndexedDB local). Si un registro de marca tiene espacios en blanco o variaciones de capitalización (ej. "Toyota" vs "Toyota " o "TOYOTA"), el carrusel genera duplicados o utiliza un término no homogeneizado, y las comparaciones con igualdad estricta (`===`) descartan los modelos que no coinciden exactamente con esa cadena específica, mostrando solo una lista parcial de modelos.
Frecuencia del problema: Intermitente / Aleatorio. Depende del orden de carga de los registros en cortes y de si se activa la sincronización silenciosa de fondo.
Condiciones de aparición: Aparece cuando la base de datos de cortes contiene registros con inconsistencias de espaciado o casing en campos de marca, modelo o categoría, y se hace clic en una marca cuyos datos no están homogeneizados.
Ubicación:
  - Archivos involucrados: `auth.js` y `ui.js`.
  - Funciones implicadas: `loadInitialData()`, `processCatalogData()`, `updateFullState()`.
  - Dependencias relacionadas: Lógica de filtrado de modelos, navegación por marca, carruseles de inicio, buscador, y sincronización en segundo plano.
Impacto: Alto
Riesgo de regresión: Bajo
Impacto sobre navegación: Alto (se corrigen listas parciales de modelos).
Impacto sobre búsqueda: Bajo.
Impacto sobre agrupaciones: Bajo.
Impacto sobre el historial: Bajo.
Estrategia de corrección:
  - Implementar una función centralizada de normalización de datos del catálogo (`normalizeCatalogData`) en `auth.js` que unifique de manera inteligente el espaciado (trim) y la capitalización de nombres de marcas, categorías y modelos en el catálogo completo.
  - Aplicar esta normalización de datos al cargar desde la caché en IndexedDB, al recibir datos de la red en segundo plano antes de guardarlos y pasarlos a la sincronización silenciosa, y en `updateFullState` de `ui.js` para asegurar que el estado y la caché siempre operen sobre un dataset 100% libre de inconsistencias y duplicidades.


===================================================================
AUDITORÍA DE REPRODUCCIÓN PERSISTENTE DE VÍDEOS EN MODALES DE TUTORIALES (22/02/2026)
===================================================================

ID del hallazgo: H-TUTORIAL-VIDEO-01
Título: Reproducción persistente de vídeos en modales de tutoriales al cerrar
Descripción: Al cerrar el modal de detalle de tutoriales, el vídeo de YouTube continúa reproduciéndose, el audio permanece activo y los recursos del reproductor no se liberan.
Causa Raíz:
  1. En `mostrarDetalleTutorialModal` (archivo `ui.js`), el iframe del vídeo se inserta utilizando un simple string replace de "watch?v=" con "embed/" en lugar del helper unificado `getYouTubeEmbedUrl`, por lo que el parámetro `enablejsapi=1` no se incluye. Esto impide que el reproductor reciba comandos del sistema (como `stopVideo`).
  2. Al cerrar el modal de detalles, el contenedor `#detalleCompleto` no destruye el iframe del reproductor, sino que simplemente se oculta la clase `.visible` de `#modalDetalle`. Como consecuencia, el iframe sigue cargado y reproduciendo contenido oculto en segundo plano.
  3. No todos los flujos de cierre (cierre manual, popstate, etc.) llaman a la detención del vídeo del tutorial, y cuando lo intentan, la API de postMessage falla debido a la falta de `enablejsapi=1`.
Ubicación:
  - Archivos: `ui.js`, `main.js`
Impacto: Alto (consumo de CPU, datos y reproducción molesta de audio en segundo plano).
Riesgo de regresión: Muy bajo.
Estrategia de corrección:
  - En `ui.js`, robustecer `mostrarDetalleTutorialModal` para usar `getYouTubeEmbedUrl(item.Video)` y establecer la propiedad `dataset.modalType = 'tutorial'` en el elemento `#modalDetalle`. Para cortes y relays, establecer `dataset.modalType` en 'corte' y 'relay' respectivamente.
  - En `main.js`, registrar un `MutationObserver` sobre `#modalDetalle` dentro de la inicialización de la app (`initializeApp`). Cuando se detecte la remoción de la clase `.visible` (modal cerrado) de forma instantánea por cualquier mecanismo de cierre, y el tipo de modal sea 'tutorial', detener completamente la reproducción, remover el iframe del DOM y liberar los recursos del reproductor de forma definitiva.

===================================================================
AUDITORÍA TÉCNICA - INSTALACIÓN Y SOPORTE DE PWA (26/02/2026)
===================================================================

ID del hallazgo: H-PWA-INSTALL-01
Título: Botón de instalación con texto y estado no dinámico
Descripción:
  1. El botón de instalación actual en `index.html` tiene el texto "Instalar" en lugar del estricto "Instala la app" solicitado.
  2. El botón de instalación se oculta/muestra basándose únicamente en el evento `beforeinstallprompt`, pero no detecta si la PWA ya está instalada o si se está ejecutando en modo standalone al iniciar la sesión o entre diferentes sesiones.
  3. No existe soporte ni detección para iOS (iPhone) Safari, el cual no soporta el evento `beforeinstallprompt` de forma nativa, dejando a los usuarios de iPhone sin la opción de instalar la app de forma coherente.
Ubicación:
  - Archivos: `index.html`, `main.js`, `style.css`
Impacto: Alto
Riesgo: Bajo
Estrategia de corrección:
  - Modificar el texto del botón a "Instala la app" en `index.html` y asegurar que no interfiera visualmente.
  - Implementar funciones de detección de instalación existente standalone (`window.matchMedia('(display-mode: standalone)').matches` and `navigator.standalone`).
  - Implementar la detección de Safari en iPhone y añadir un modal instructivo e interactivo personalizado para guiar al usuario en la instalación nativa.
  - Guardar el estado de la instalación/omisión de sesión en `sessionStorage` para cumplir con las especificaciones de no volver a mostrar el botón si ya está instalado o si se completó en la sesión actual.

===================================================================
AUDITORÍA DE DIRECCIONAMIENTO DE ENLACES DE MODELOS COMPARTIDOS (30/07/2024)
===================================================================

ID del hallazgo: H-SHARE-DEEPLINK-01
Título: El enlace compartido de un modelo en el modal de detalle no se dirige al modelo específico, sino a la lista de generaciones del modelo
Descripción: Al compartir la información de un modelo desde el modal de detalles, el botón de compartir genera un enlace que contiene la marca y el modelo como parámetros de búsqueda, pero no incluye el año de inicio (anoDesde) del modelo específico. Cuando se hace clic en el enlace, el sistema realiza una búsqueda de marca y modelo que devuelve múltiples generaciones de ese vehículo. Al haber más de un resultado, no se abre el modal de detalles del modelo de forma automática, sino que se muestra la tarjeta del modelo, y al hacer clic en ella, el usuario es redirigido a la lista de generaciones (años) en lugar de ver el detalle del modelo específico compartido.
Ubicación:
  - Archivo: `ui.js` (mostrarDetalleModal)
Impacto: Alto (Afecta negativamente la experiencia del usuario al recibir un enlace compartido, no pudiendo visualizar directamente la información técnica del vehículo esperado).
Riesgo de regresión: Muy bajo.
Estrategia de corrección:
  - Modificar el url del objeto shareData generado al hacer clic en el botón Compartir en `ui.js`.
  - Construir un término de búsqueda concatenando `marca`, `modelo`, `versionesAplicables` (si existe), `tipoEncendido` (si existe) y `anoDesde`.
  - Filtrar las propiedades vacías para asegurar que se cree una consulta limpia y bien estructurada.
  - Esto restringirá con absoluta precisión los resultados de búsqueda a una sola generación y variante específica del vehículo. Al tener un único resultado exacto en `filtrarContenido`, la aplicación abrirá de forma inmediata e interactiva el modal de detalle para el modelo específico compartido originalmente.

--------------------------------------------------

**AUDITORÍA TÉCNICA - Implementación de aviso de aceptación de condiciones para primer inicio de sesión (03/03/2026)**

**Archivos involucrados:** index.html, ui.js, main.js, style.css.

**Hallazgos:**
1. **Flujo de Inicio de Sesión y de Inicialización (auth.js / ui.js):**
   - En `auth.js`, al completar la validación o el inicio de sesión correcto, se llama a `showApp(user)`.
   - En `ui.js`, la función `showApp(user)` oculta la pantalla de carga (`#splash-screen`), muestra el catálogo principal (`.container` y `.footer`) y renderiza las categorías.
   - Si un usuario accede por primera vez en un dispositivo dado, el aviso de términos y condiciones debe bloquear la pantalla e impedir que el usuario interactúe con el catálogo o lo use.
   - El punto más adecuado para verificar el aviso es inmediatamente después del éxito en `showApp(user)`, pero antes de renderizar la interactividad completa del catálogo, o bien mostrando un modal ineludible sobrepuesto sobre `.container` y ocultando el resto de controles de navegación de la app.

2. **Caché y Almacenamiento Local (localStorage / IndexedDB):**
   - La plataforma utiliza `localStorage` activamente para almacenar preferencias de la sesión actual (`gpsepedia_session`, `darkMode`, etc.) e IndexedDB (`offline.js`) para persistencia local de catálogo, historial de búsqueda, visto recientemente y validationCache.
   - El uso de `localStorage` con la clave `gpsepedia_terms_accepted` es el mecanismo más robusto y de persistencia rápida en el frontend solicitado para almacenar de manera unificada la aceptación del aviso.

3. **Logotipos y Tema de Color (main.js / style.css):**
   - En `main.js`, la función `updateLogos` intercambia dinámicamente las rutas del logotipo principal y del modal según el modo oscuro o claro (utilizando `Logo_TemaOscuro.png` o la url clásica de Drive).
   - Necesitamos asegurar que el logotipo del modal de términos (`.terms-logo`) sea incluido en el mecanismo de actualización dinámica de logos de `main.js` para mantener la consistencia estética.

4. **Compatibilidad y Restricciones:**
   - No se debe modificar el backend ni el sistema de autenticación de `auth.js`.
   - El aviso debe ser modal, ineludible, con el texto exacto requerido, centrado, con logotipo oficial con proporciones originales y tamaño reducido.
   - Una vez aceptados los términos por primera vez, se guarda la preferencia localmente y se permite el acceso total al catálogo, impidiendo que vuelva a presentarse en recargas o nuevas aperturas.

--------------------------------------------------

**AUDITORÍA TÉCNICA - Enlace de Acuerdo de Uso, Privacidad y Deslinde de Responsabilidad en el Footer (03/03/2026)**

**Archivos involucrados:** index.html, ui.js, main.js, style.css.

**Hallazgos:**
1. **Estructura y estilos del pie de página (`.footer` en index.html / style.css):**
   - El footer se ubica al fondo del DOM con un sentinel de IntersectionObserver.
   - Contiene la clase `.footer` y un contenedor `.footer-links` con los enlaces existentes: Sobre nosotros, Contáctanos y Preguntas frecuentes.
   - Los estilos en `style.css` aplican colores, bordes, márgenes y una tipografía específica a los enlaces (`.footer-links a`).
   - El nuevo enlace "Acuerdo de Uso" debe añadirse dentro del contenedor `.footer-links` conservando la jerarquía exacta de estilos sin alterar el tamaño, altura, colores ni tipografías existentes.

2. **Visualización en Modal (ui.js / index.html):**
   - El acuerdo debe ser completamente legible y desplazable de forma interactiva en un modal del mismo diseño visual que los otros modales de información de la plataforma (como `#about-us-modal`, `#faq-modal`, etc.).
   - Utilizaremos la clase `.info-modal` y `.info-modal-content` en un nuevo modal `#terms-agreement-modal`.
   - Añadiremos un botón de cierre, cabecera y el contenido estructurado del Acuerdo legal provisto por el usuario.
   - Vincularemos el modal con el método `setupModal` de `ui.js` para controlar su apertura y cierre de forma idéntica a los otros modales legales.

--------------------------------------------------

**AUDITORÍA TÉCNICA - Ajuste de Integración del Enlace de Términos y Condiciones en el Aviso Inicial (03/03/2026)**

**Archivos involucrados:** index.html, ui.js, main.js, style.css.

**Hallazgos:**
1. **Renombrar enlace del footer (index.html / main.js):**
   - El enlace permanente de Acuerdo Legal en el footer debe renombrarse de "Acuerdo de Uso y Privacidad" a exactamente "Términos y Condiciones".
   - Esto requiere modificar el texto del enlace `#footer-terms-link` en `index.html`. No se altera su ID, estilos ni lógica.

2. **Agregar enlace interactivo en el aviso inicial (index.html / main.js):**
   - El aviso inicial `#terms-modal` debe cambiar el texto "Al continuar, usted acepta estos términos." a "Al continuar, usted acepta los Términos y Condiciones.".
   - La cadena "Términos y Condiciones" debe ser un elemento `<a>` interactivo con ID `terms-link-in-modal`, estilizado con color de acento y subrayado.
   - Al hacer clic, debe abrirse el modal legal `#terms-agreement-modal` mediante `ui.openTermsAgreement()`, manteniendo el aviso inicial visible en el fondo y sin registrar la aceptación.

3. **Ineludibilidad del Aviso Inicial (main.js / ui.js):**
   - El aviso inicial no debe poder cerrarse por ningún método externo (botón X, clic fuera, popstate/gesto de retroceso).
   - Para esto, debemos:
     a) Asegurar que `#terms-modal` no tenga ningún botón X de cierre.
     b) Bloquear el listener global de `popstate` de manera absoluta cuando `#terms-modal` esté visible para ignorar retrocesos y evitar el cierre de la app o modal de fondo.
     c) Condicionar que el único disparador de cierre sea pulsar el botón "Aceptar Términos", el cual efectúa el guardado de la preferencia en `localStorage` y desbloquea el catálogo.

4. **Estratificación de Capas Z-Index (style.css):**
   - El modal legal `#terms-agreement-modal` debe tener un `z-index: 3200` mayor que el de `#terms-modal` (`z-index: 3100`), garantizando que se muestre en primer plano de manera limpia y sin conflictos lógicos cuando se abre desde el aviso inicial.

--------------------------------------------------

**AUDITORÍA TÉCNICA - Ajuste de Enlaces Legales y Optimización Responsive Móvil de Footer y Modales (03/03/2026)**

**Archivos involucrados:** index.html, ui.js, main.js, style.css.

**Hallazgos:**
1. **Enlace de Términos y Condiciones en login modal (index.html / main.js):**
   - El login modal `#login-modal` cuenta con una sección de enlaces `.footer-links` al final de su contenido con enlaces a "Sobre Nosotros", "Contáctanos" y "Preguntas Frecuentes".
   - El requisito pide agregar el enlace "Términos y Condiciones" dentro de esta lista de enlaces en `#login-modal` en `index.html`.
   - Al seleccionarlo, debe abrir `#terms-agreement-modal` con `ui.openTermsAgreement()` de manera ininterrumpida sin cerrar el modal de login. Al cerrar el modal legal, el usuario regresa automáticamente al modal de inicio de sesión.

2. **Justificación de textos legales (style.css):**
   - El cuerpo de texto del modal de aviso inicial (`#terms-modal .terms-body`) y el cuerpo de texto del modal legal (`#terms-agreement-modal p`, `#terms-agreement-modal li`) deben estar completamente justificados (`text-align: justify;`).

3. **Renombrar el título del modal legal (index.html):**
   - El título principal de `#terms-agreement-modal` en `index.html` debe cambiarse de "Acuerdo de Uso, Privacidad y Deslinde de Responsabilidad de GPSpedia" a exactamente "Acuerdo de uso, términos y condiciones".

4. **Optimización del aviso inicial `#terms-modal` para no tener scroll vertical (style.css):**
   - En móviles y pantallas de baja resolución, `#terms-modal` puede desbordarse o generar scroll.
   - Debemos ajustar el modal para que todas las secciones (logo centrado, título "Aviso Importante", cuerpo de texto justificado y botón "Aceptar Términos") quepan de forma compacta y responsive sin generar barras de desplazamiento vertical. Se reducirán márgenes internos, rellenos y espacios, y la tipografía se adaptará de forma fluida.

5. **Compactación del footer móvil (style.css):**
   - En la vista móvil/PWA, debido al tamaño de la pantalla, el nuevo enlace "Términos y Condiciones" salta a una segunda fila.
   - Esto incrementa la altura del footer. Debemos compactar el footer disminuyendo el espacio vertical (`margin` o `padding`) entre la primera fila de enlaces y la segunda fila, y conservar la distribución limpia y unificada de los elementos de derechos reservados.

===================================================================
AUDITORÍA DE ACTUALIZACIÓN DE CONTENIDO Y ESTILOS DE MODALES (04/03/2026)
===================================================================

ID del hallazgo: H-MODAL-01
Título: Actualización del contenido y estilos del modal "Sobre Nosotros"
Descripción: El contenido del modal `#about-us-modal` contiene emojis, iconos y un formato informal. Se requiere reescribir por completo el contenido utilizando una redacción puramente institucional enfocada en la consulta técnica, alineada a la izquierda y completamente libre de elementos visuales/decorativos no solicitados.
Ubicación:
  - Archivo: index.html (líneas 517-538)
  - Archivo: style.css
Impacto: Medio
Riesgo: Bajo
Dependencias: `#about-us-modal`

ID del hallazgo: H-MODAL-02
Título: Ajuste de alineación y separación en el modal "Términos y Condiciones"
Descripción:
  1. El contenido del modal legal `#terms-agreement-modal` está justificado. Se requiere cambiar a alineación a la izquierda (`text-align: left`) para títulos, subtítulos, listas y párrafos, manteniendo su estructura legal y legibilidad.
  2. El logo de TRACKLINK (`tracklink_logo.png`) al final de la página de términos debe tener mayor separación vertical con respecto a la última línea de texto para lograr una distribución visual equilibrada.
  3. El logo de GPSpedia al inicio del modal legal debe mantenerse en la parte superior (sin compartir espacio horizontal con el de TRACKLINK) e incrementar ligeramente su tamaño, manteniendo sus sombras tipo `drop-shadow` y diseño limpio sin contenedores visibles.
Ubicación:
  - Archivo: index.html (líneas 301-514)
  - Archivo: style.css
Impacto: Medio
Riesgo: Bajo
Dependencias: `#terms-agreement-modal`, `.gpsepedia-logo`, `.tracklink-logo`

ID del hallazgo: H-MODAL-03
Título: Ajuste de alineación del texto del aviso inicial (Aviso Importante)
Descripción: El texto del modal de aviso inicial `#terms-modal` debe estar centrado horizontalmente (`text-align: center`) en lugar de justificado. No se debe modificar su comportamiento ni caché de aceptación.
Ubicación:
  - Archivo: style.css
Impacto: Medio
Riesgo: Bajo
Dependencias: `#terms-modal .terms-body`


===================================================================
AUDITORÍA TÉCNICA - Registro de modelos sin información de corte, prevención de duplicados y recuperación ante interrupciones (08/08/2026)
===================================================================

ID del hallazgo: H-REG-01
Título: Registro de nuevos modelos sin información de corte ("No recomendado")
Descripción:
  1. En `add_cortes.html`, cuando el usuario selecciona "No recomendado" como tipo de corte, los campos Configuración de relay (`configRelay1`), Descripción del corte (`ubicacionCorte1`), Colores de cable (`colorCableCorte1`) y Fotografía del corte (`imgCorte1`) deben bloquearse (deshabilitarse) y dejar de ser obligatorios.
  2. Actualmente, `configRelay1` e `imgCorte1` tienen el atributo `required` en el HTML de `add_cortes.html`. Debemos remover el atributo `required` y deshabilitar los elementos de manera dinámica cuando `tipoCorte1 === "No recomendado"`.
  3. La fotografía del modelo del vehículo (`imagenVehiculo`) debe seguir siendo obligatoria para nuevos vehículos, incluso si el tipo de corte es "No recomendado".
Ubicación:
  - Archivo: add_cortes.html (form-stage-2)
Impacto: Alto
Riesgo: Bajo
Dependencias: `tipoCorte1`, `configRelay1`, `ubicacionCorte1`, `colorCableCorte1`, `imgCorte1`, `imagenVehiculo`

ID del hallazgo: H-DUP-02
Título: Prevención de duplicados mediante validación silenciosa e idempotencia
Descripción:
  1. Las interrupciones de red durante la segunda etapa (pérdida de conexión, suspensión, `Fail to fetch`) pueden provocar que un registro ya procesado en el servidor se vuelva a enviar, creando filas duplicadas e imágenes huérfanas en Drive.
  2. Para solucionar esto, implementaremos una validación silenciosa en el servidor. Al llamar a `addOrUpdateCut`, el servidor buscará si ya existe un registro con el ID de operación único (`opId`) que el cliente genera y asocia a la solicitud.
  3. Almacenaremos el `opId` de manera transparente en la columna del colaborador concatenando ` [${opId}]` a su nombre (por ejemplo, `Juan Perez [OP-XXXXXXXXXXXX]`).
  4. Si el servidor detecta que el `opId` ya existe en la hoja de cálculo, no creará una nueva fila, sino que devolverá la información existente (`vehicleId` y `timestamp`), y reparará cualquier archivo que falte o esté dañado (comprobación `checkFileIdValid`).
Ubicación:
  - Archivos: Services/write.js, Services/admin.js, add_cortes.html
Impacto: Crítico
Riesgo: Medio
Dependencias: `addOrUpdateCut`, `addSupplementaryInfo`, `colaborador`

ID del hallazgo: H-REC-03
Título: Persistencia temporal del estado y recuperación automática
Descripción:
  1. El cliente debe guardar el estado completo del formulario y archivos en `localStorage` bajo la clave `gpsepedia_pending_operation` cuando inicia el procesamiento de la etapa 2.
  2. Al iniciarse la aplicación o reanudarse la conectividad (`online` event), el sistema comprobará si hay una operación pendiente de recuperación. Si es así, consultará silenciosamente al servidor mediante una nueva acción `checkOperation` para verificar si la operación se completó en el servidor.
  3. Si se completó con éxito (o parcialmente), reanudará el flujo desde el paso correspondiente sin requerir acción del usuario y sin duplicar registros. Si faltan archivos, los re-subirá automáticamente usando el base64 guardado.
Ubicación:
  - Archivos: add_cortes.html, Services/write.js, Services/admin.js, api-config.js
Impacto: Alto
Riesgo: Medio
Dependencias: `init`, `online`, `localStorage`

===================================================================
AUDITORÍA DE DIAGNÓSTICO PROFUNDO EN REGISTRO DE NUEVOS MODELOS (10/08/2026)
===================================================================

ID del hallazgo: H-DIAG-01
Título: QuotaExceededError al persistir en localStorage imágenes de alta resolución
Descripción: En `add_cortes.html`, la función `handleStage2Submit` recopila y convierte las imágenes en base64 de alta resolución para el vehículo y el corte. Inmediatamente antes de guardarlo en `localStorage` con la clave `gpsepedia_pending_operation`, el sistema no realiza una captura segura ni controlada de posibles errores de cupo (`QuotaExceededError`). Al dispararse este error, la ejecución de la función se bloquea totalmente y la petición asíncrona de guardado `addOrUpdateCut` nunca llega a invocarse en el servidor, provocando la falla silenciosa del proceso de guardado para imágenes grandes.
Ubicación:
  - Archivo: `add_cortes.html` (Línea 828-831 aprox.)
Impacto: Crítico
Riesgo de regresión: Muy bajo.
Estrategia de corrección:
  - Envolver el guardado del estado en localStorage en un bloque `try-catch` robusto para hacer una captura segura del error `QuotaExceededError`. Si ocurre, emitir una advertencia a la consola de diagnóstico y continuar de manera transparente con el guardado en el servidor.

ID del hallazgo: H-DIAG-02
Título: Disparidad de saneamiento e inconsistencia de ID en registro de vehículos en write.js
Descripción: Al registrar un vehículo nuevo en `Services/write.js` (a diferencia del microservicio `Services/admin.js`), no se realiza el limpiado explícito y reset de la celda de ID de la fila copiada. Esto puede provocar colisiones de IDs redundantes o nulos, impidiendo que el backend devuelva el `vehicleId` recién calculated de manera consistente.
Ubicación:
  - Archivo: `Services/write.js` (Líneas 270-280 aprox.)
Impacto: Alto
Riesgo de regresión: Bajo
Estrategia de corrección:
  - Copiar la lógica de control del ID implementada con éxito en `Services/admin.js` para limpiar y volver a calcular la celda de ID usando la fórmula de la fila anterior o un fallback `=ROW()-1`.

===================================================================
AUDITORÍA TÉCNICA - Normalización de Marcas, Modelos y Versiones (22/02/2026)
===================================================================

ID del hallazgo: H-NORM-01
Título: Sensibilidad a mayúsculas/minúsculas y formatos duplicados en catálogo
Descripción: Al registrar un vehículo o corte en `Services/write.js` y `Services/admin.js`, las marcas, modelos y versiones de equipamiento se registran tal como las escribe el usuario. Esto provoca registros duplicados (como "RAM" y "Ram", o "Mercedes-Benz" y "mercedes benz").
Ubicación:
  - Archivo: `Services/write.js`
  - Archivo: `Services/admin.js`
Impacto: Crítico (Genera inconsistencias de catálogo y fragmentación de datos)
Riesgo de regresión: Bajo
Estrategia de corrección:
  - Implementar lógica unificada de generación de claves de comparación (`toComparisonKey`) y normalización para almacenamiento (`normalizeForStorage`).
  - Buscar y reutilizar el formato existente en el spreadsheet si se detecta una entidad equivalente para marcas, modelos y versiones de equipamiento.

ID del hallazgo: H-NORM-02
Título: Falta de contexto de marca en sugerencias de modelos y tolerancia aproximada laxa
Descripción:
  1. En `Services/write.js` and `Services/catalog.js`, `handleGetSuggestion` busca sugerencias en toda la columna de modelos sin considerar la marca seleccionada, lo que puede resultar en sugerencias cruzadas irrelevantes.
  2. Al escribir un modelo similar a uno existente con diferencias menores de separación, no se utiliza una equivalencia estricta por claves, lo que puede dar falsos negativos.
Estrategia de corrección:
  - Modificar `handleGetSuggestion` en el backend para recibir opcionalmente la marca y priorizar/filtrar las sugerencias de modelos de esa marca.
  - Implementar umbral adaptativo para Levenshtein y equivalencia exacta de claves de comparación antes de sugerir.
  - Modificar `handleSuggestion` en `add_cortes.html` para enviar la marca seleccionada al solicitar sugerencias de modelos.

--------------------------------------------------
**AUDITORÍA TÉCNICA - SOPORTE PARA MÚLTIPLES CABLES DE ALIMENTACIÓN POR MODELO (13/08/2026)**

**Archivos involucrados:** Services/write.js, Services/admin.js, Services/catalog.js, Services/feedback.js, add_cortes.html, ui.js, style.css, ChangesLogs.txt, index.html.

**Hallazgos:**
1. **Mapeo de Columnas:**
   - La estructura de columnas de la hoja `Cortes` tiene `apertura: 33`, `imgApertura: 34`, `cableAlimen: 35`, `imgCableAlimen: 36`, `timestamp: 37`, `notaImportante: 38`.
   - Añadiremos soporte para `cableAlimen2: 39`, `imgCableAlimen2: 40`, `cableAlimen3: 41`, `imgCableAlimen3: 42` al final del registro.
   - En todos los archivos de backend (`write.js`, `admin.js`, `catalog.js`, `feedback.js`), extenderemos el objeto `COLS_CORTES`.

2. **Procesamiento de Imágenes en el Backend:**
   - En `Services/admin.js`, las funciones como `getActiveImageUrls`, `validateAndRestoreAllTrashedImagesInSpreadsheet`, `cleanUpDuplicatesInBrandTree`, and `handleUploadAdminImage` escanean o procesan campos de imágenes en listas explícitas como `imgFields` o `colIndices`.
   - Añadiremos `imgCableAlimen2` (col 40 / índice 39) y `imgCableAlimen3` (col 42 / índice 41) a estas listas.
   - Definiremos `typeFuncion = "_alimen2"` y `typeFuncion = "_alimen3"` respectivamente.

3. **Inmutabilidad de Timestamp:**
   - Al registrar información suplementaria mediante `handleAddSupplementaryInfo` (en `write.js` and `admin.js`), se actualiza el `timestamp` por defecto.
   - El requisito exige que agregar un nuevo cable de alimentación no altere el timestamp existente.
   - Solución: En `handleAddSupplementaryInfo`, verificaremos si la fila ya contiene un valor de timestamp válido (`currentTimestamp = rowValues[COLS_CORTES.timestamp - 1]`). Si es así, no sobrescribiremos el timestamp.

4. **Formulario de Registro (Stage 3 en `add_cortes.html`):**
   - Calcularemos dinámicamente el próximo slot de cable libre (1, 2, o 3) en `proceedToStage3(vehicleId)` usando el registro coincidente en `state.matches`.
   - Si ya existen cables en slots 1 y 2, el slot destino será el 3. Si ya existen en slots 1, 2 y 3, deshabilitaremos la sección de cables y mostraremos un mensaje indicando el límite alcanzado.
   - Actualizaremos los textos del formulario (ej. "Cable de Alimentación 2") dinámicamente.
   - Al guardar, mapearemos las variables `cableAlimen` e `imgCableAlimen` del formulario a las propiedades del slot correcto (`cableAlimen2`/`imgCableAlimen2` o `cableAlimen3`/`imgCableAlimen3`).

5. **Modal de Detalle y Carrusel Responsivo (`ui.js` & `style.css`):**
   - Recopilaremos todos los cables de alimentación existentes (hasta 3) en `mostrarDetalleModal`.
   - Si solo hay 1 cable, se mantendrá el diseño actual estático y sin botones de navegación.
   - Si hay 2 o 3 cables, se inicializará un carrusel interactivo que actualiza simultáneamente la descripción y la imagen.
   - En escritorio, se mostrarán botones `<` y `>` para la navegación.
   - En móvil y PWA, se ocultarán los botones `<` y `>` usando CSS responsivo y `@media (display-mode: standalone)`, y se implementarán gestos táctiles nativos (`touchstart`/`touchend`) para cambiar de slide.
   - Se adaptará el modo de edición in-modal para desarrolladores para que puedan visualizar y editar de forma independiente las 3 posiciones de cables e imágenes.

6. **Incremento de Versión y Changelog:**
   - Incrementaremos la versión global del sistema a `2.1.6` en `index.html`, etc.
   - Documentaremos detalladamente los cambios en `ChangesLogs.txt` y en el modal `#changelog-modal`.

--------------------------------------------------
**AUDITORÍA TÉCNICA - Registro de modelos sin información de corte, prevención de duplicados y recuperación ante interrupciones (16/08/2026)**

**Archivos involucrados:** add_cortes.html, Services/write.js, Services/admin.js

**Hallazgos de Auditoría:**
1. **add_cortes.html**:
   - `toggleNoRecomendado` ya existe en el frontend y gestiona la deshabilitación y remoción del atributo `required` para configRelay1, ubicacionCorte1, colorCableCorte1, e imgCorte1.
   - Sin embargo, para que el backend funcione robustamente, si el usuario selecciona "No recomendado", los campos se deben inyectar como vacíos, y la imagen del corte principal (`imgCorte1`) no debe ser requerida ni enviada.
   - En `handleStage2Submit`, la verificación silenciosa se inicia mediante `checkOperation` para comprobar si existe una operación con el mismo ID de operación (`opId`).
   - Pero si `checkRes.exists` es verdadero, el script *aún* llama a `addOrUpdateCut` en lugar de saltar directamente a la Fase 3 con el vehículo recuperado. Esto anula la protección de duplicados y causa re-subidas lentas innecesarias o filas duplicadas si no se maneja en el backend.
   - **Solución**: Si `checkRes.exists` es `true` dentro de `handleStage2Submit`, debemos proceder directamente a la Fase 3 recuperando la información del vehículo y timestamp.

2. **Visibilidad en segundo plano y bloqueo de pantalla (visibilitychange)**:
   - El evento `visibilitychange` no está actualmente registrado para realizar una comprobación automática de recuperación cuando el usuario vuelve a la PWA.
   - **Solución**: Registrar un listener para `visibilitychange` en `add_cortes.html`. Si la página se vuelve visible (`document.visibilityState === 'visible'`), invocar silenciosamente `checkAndRecoverPendingOperation()`.

3. **Manejo de Errores de Conectividad (Network Error vs Server Error)**:
   - En `handleStage2Submit`, todos los errores se muestran como una alerta genérica con el mensaje original.
   - El error "Fail to fetch" o errores de red no diferencian si la conexión se interrumpió o si la solicitud fue rechazada por el servidor.
   - **Solución**: Capturar de manera específica errores que contengan "fetch" o "network", o cuando `navigator.onLine === false`, mostrando un mensaje claro al usuario indicando que el progreso se guardó localmente y que el sistema comprobará el estado automáticamente cuando vuelva la conexión.

4. **Integridad del Backend (Services/write.js y Services/admin.js)**:
   - En `handleAddOrUpdateCut`, cuando un `opId` existente es detectado, actualmente repara y valida las imágenes del vehículo y el corte, pero no valida ni completa de forma progresiva campos faltantes si el registro quedó parcialmente completado (por ejemplo, si faltan metadatos como configuración de relay o ubicación).
   - **Solución**: Enriquecer la lógica del backend para completar e integrar campos faltantes en el registro existente si se detecta que el registro ya existe por su `opId`, preservando la información que ya existe y actualizando únicamente lo faltante.

5. **Respaldos**:
   - Se realizarán respaldos de `add_cortes.html`, `Services/write.js`, y `Services/admin.js` antes de cualquier modificación.

--------------------------------------------------
**AUDITORÍA TÉCNICA - Navegación del carrusel de cables de alimentación dentro del lightbox (16/08/2026)**

**Archivos involucrados:** lightbox.js, ui.js, index.html, style.css.

**Hallazgos de Auditoría:**
1. **lightbox.js**:
   - `abrirLightbox(url, imgId)` actualmente solo acepta la URL de una imagen individual y no almacena contexto de carrusel ni lista de imágenes navegables.
   - Para permitir la navegación entre las imágenes de los cables de alimentación, se debe extender la firma a `abrirLightbox(url, imgId, context)`, donde `context` contendrá `{ cables: [...], currentIndex: number, onSlideChange: function }`.
   - Se requiere implementar la detección dinámica del estado de zoom mediante `isLightboxZoomed()`, evaluando `window.visualViewport.scale > 1.02` y posibles transformaciones CSS de escala.
   - En versión móvil/PWA, la navegación debe ser por gestos de deslizamiento horizontal (`touchstart`, `touchmove`, `touchend`).
   - REQUISITO CRÍTICO DE ZOOM: Cuando `isLightboxZoomed()` sea `true` (imagen ampliada), los gestos horizontales NO deben cambiar de imagen ni interferir con el desplazamiento de la imagen. La navegación mediante gestos solo debe reactivarse cuando la imagen vuelva a su tamaño predeterminado (`scale === 1.0`).
   - Para evitar cierres involuntarios del Lightbox tras un gesto de deslizamiento, se debe implementar la bandera `ignoreNextClick`.

2. **ui.js**:
   - En el renderizado del carrusel de cables de alimentación (líneas 3240-3360 aprox.), la interacción de clic en las imágenes de los cables (`slideImg.onclick` e `img.onclick`) invoca `abrirLightbox` pasando solo la URL de alta resolución.
   - Se debe actualizar esta llamada para filtrar los cables de alimentación registrados que cuentan con imagen válida (`sec.cables.filter(c => c.img)`), determinando el índice del cable seleccionado y enviando la lista y el callback de sincronización `onSlideChange` a `abrirLightbox`.

3. **index.html**:
   - El contenedor `#lightbox` no dispone de botones de navegación en el marcado.
   - Se deben agregar los botones semitransparentes `#lightboxPrevBtn` y `#lightboxNextBtn` dentro de `#lightbox`.
   - El manejo de clics debe delegarse limpiamente desde `lightbox.js` para evitar cierres accidentales o interferencias con el zoom.

4. **style.css**:
   - Se deben incorporar los estilos para `.lightbox-nav-btn` con posicionamiento absoluto centrado verticalmente, fondo semitransparente, bordes redondeados y sombra.
   - Se deben aplicar las reglas de media query `@media (max-width: 768px)` y `@media all and (display-mode: standalone)` para ocultar los botones de escritorio en dispositivos móviles/PWA, garantizando que en dichas plataformas la navegación sea exclusivamente táctil.

--------------------------------------------------
**AUDITORÍA TÉCNICA - Diagnóstico y corrección de la acción 'getUsers' desconocida (17/08/2026)**

**Archivos involucrados:** api-config.js, Services/users.js, users.html.

**Hallazgos de Auditoría:**
1. **api-config.js**:
   - En `ACTION_TO_SERVICE_MAP`, las acciones relativas a la administración de usuarios (`getUsers`, `createUser`, `updateUser`, `deleteUser`, `changePassword`, `updateProfile`) están mapeadas al servicio `USERS`.
   - Sin embargo, en `API_ENDPOINTS.USERS`, la URL asignada (`https://script.google.com/macros/s/AKfycbxpDSLvKqb2taxKS0PbGqhTPxgO01lJyGsDn1Tgpe4zxZii8QWnvlvj5Xltrn6Vmkf6/exec`) corresponde en realidad a un despliegue de `Write-SERVICE` (`GPSpedia Write-SERVICE v2.0`).
   - Al realizar una petición POST con `action: 'getUsers'` hacia este endpoint, el despachador `doPost` de `Write-SERVICE` (ubicado en `Services/write.js`) evalúa la acción `getUsers`, no la encuentra registrada en sus casos de `switch`, y lanza la excepción: `La acción 'getUsers' es desconocida.`

2. **Services/users.js**:
   - El archivo `Services/users.js` sí contiene la definición adecuada de `getUsers` y de `handleGetUsers(payload)`, así como el control de privilegios RBAC correspondiente (`['Desarrollador', 'Gefe', 'Jefe', 'Supervisor']`).
   - La acción `getUsers` existe y está correctamente implementada en el código del microservicio `users.js`.

3. **Estrategia de Corrección**:
   - Para resolver la discrepancia de forma definitiva, se requiere actualizar `API_ENDPOINTS.USERS` en `api-config.js` apuntando a la URL del despliegue activo del servicio `Users-SERVICE`.
   - Se creará primero un respaldo de `api-config.js` previo a la modificación, se verificará la respuesta tras la corrección y se validará la integridad y seguridad del servicio.
